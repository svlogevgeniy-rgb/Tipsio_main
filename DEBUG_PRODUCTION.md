# 🔍 Диагностика ошибки "Internal server error" при регистрации

## ✅ Что добавлено для диагностики:

### 1. Health Check Endpoint
**URL:** `http://5.129.242.61/api/health`

Проверяет:
- ✅ Подключение к базе данных
- ✅ Наличие переменных окружения
- ✅ Работу Prisma Client
- ✅ Количество пользователей и venues

### 2. Детальное логирование ошибок
В API регистрации теперь логируются:
- Тип ошибки (name, code)
- Сообщение ошибки
- Стек трейс
- Prisma-специфичные ошибки
- Ошибки подключения к БД

## 🚀 Шаги для диагностики на продакшн:

### Шаг 1: Обновите код на сервере
```bash
ssh user@5.129.242.61
cd /path/to/tipsio
git pull origin main
npx prisma generate
npm run build
pm2 restart tipsio
```

### Шаг 2: Проверьте Health Check
```bash
curl http://5.129.242.61/api/health | jq .
```

**Ожидаемый результат (если всё ОК):**
```json
{
  "timestamp": "2025-12-03T20:48:41.533Z",
  "env": {
    "NODE_ENV": "production",
    "DATABASE_URL_SET": true,
    "NEXTAUTH_SECRET_SET": true
  },
  "prisma": {
    "connected": true,
    "error": null
  },
  "database": {
    "userCount": 5,
    "venueCount": 3,
    "error": null
  }
}
```

### Шаг 3: Проверьте логи сервера
```bash
# PM2:
pm2 logs tipsio --lines 100

# Docker:
docker logs tipsio-container --tail 100

# Systemd:
journalctl -u tipsio -n 100
```

**Ищите строки:**
- `[Registration] Starting registration process`
- `[Registration] Prisma client available: true`
- `[Registration] DATABASE_URL set: true`
- `[Registration] Error:` (если есть ошибка)

### Шаг 4: Попробуйте зарегистрироваться через curl
```bash
curl -X POST http://5.129.242.61/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123456",
    "venueName": "Test Cafe",
    "venueType": "CAFE"
  }' -v
```

## 🐛 Возможные проблемы и решения:

### Проблема 1: Health Check показывает `"connected": false`

**Причина:** Проблема с подключением к базе данных

**Решение:**
```bash
# Проверьте DATABASE_URL
cat .env | grep DATABASE_URL

# Проверьте, запущен ли PostgreSQL
sudo systemctl status postgresql

# Проверьте подключение вручную
psql $DATABASE_URL -c "SELECT 1"
```

### Проблема 2: Health Check показывает `"DATABASE_URL_SET": false`

**Причина:** Переменная окружения не установлена

**Решение:**
```bash
# Добавьте в .env
echo 'DATABASE_URL="postgresql://user:password@localhost:5432/tipsio"' >> .env

# Перезапустите приложение
pm2 restart tipsio
```

### Проблема 3: Логи показывают "Prisma client available: false"

**Причина:** Prisma Client не сгенерирован

**Решение:**
```bash
npx prisma generate
npm run build
pm2 restart tipsio
```

### Проблема 4: Логи показывают "P2002: Unique constraint failed"

**Причина:** Пользователь с таким email уже существует

**Решение:**
```bash
# Используйте другой email или удалите существующего пользователя
npx prisma studio
# Найдите и удалите пользователя через UI
```

### Проблема 5: Логи показывают "ECONNREFUSED"

**Причина:** База данных недоступна

**Решение:**
```bash
# Проверьте, запущен ли PostgreSQL
sudo systemctl status postgresql

# Запустите PostgreSQL
sudo systemctl start postgresql

# Проверьте порт
netstat -tlnp | grep 5432
```

### Проблема 6: Логи показывают "bcrypt" ошибку

**Причина:** Проблема с bcryptjs

**Решение:**
```bash
# Переустановите зависимости
rm -rf node_modules package-lock.json
npm install
npm run build
pm2 restart tipsio
```

## 📊 Чек-лист диагностики:

- [ ] Код обновлён (`git pull`)
- [ ] Prisma Client сгенерирован (`npx prisma generate`)
- [ ] Проект собран (`npm run build`)
- [ ] Приложение перезапущено (`pm2 restart`)
- [ ] Health Check возвращает 200 OK
- [ ] `prisma.connected = true`
- [ ] `DATABASE_URL_SET = true`
- [ ] `userCount` и `venueCount` не null
- [ ] Логи не показывают ошибок

## 🎯 Что делать после проверки:

### Если Health Check показывает ошибку:
1. Скопируйте вывод `curl http://5.129.242.61/api/health`
2. Скопируйте логи сервера (последние 50 строк)
3. Отправьте мне для анализа

### Если Health Check OK, но регистрация не работает:
1. Попробуйте зарегистрироваться через браузер
2. Откройте DevTools (F12) → Console
3. Скопируйте все логи из Console
4. Скопируйте логи сервера во время попытки регистрации
5. Отправьте мне для анализа

## 📞 Быстрая проверка (одна команда):

```bash
echo "=== Health Check ===" && \
curl -s http://5.129.242.61/api/health | jq . && \
echo "" && \
echo "=== Server Logs ===" && \
pm2 logs tipsio --lines 20 --nostream
```

Эта команда покажет:
1. Статус health check
2. Последние 20 строк логов

Отправьте мне результат, и я точно скажу, в чём проблема!
