# 🚀 Tipsio Deployment Guide

## Скрипты для деплоя

В проекте есть три скрипта для упрощения деплоя и диагностики:

### 1. `deploy.sh` - Полный деплой с проверками
Интерактивный скрипт с пошаговым выполнением и проверками.

```bash
./deploy.sh
```

**Что делает:**
- ✅ Обновляет код из git
- ✅ Устанавливает зависимости
- ✅ Генерирует Prisma Client
- ✅ Спрашивает про миграции БД
- ✅ Собирает проект
- ✅ Перезапускает приложение (PM2/Docker/systemd)
- ✅ Проверяет health check
- ✅ Показывает детальную информацию

**Используйте когда:**
- Первый деплой
- Важное обновление
- Нужна детальная информация о процессе

---

### 2. `quick-deploy.sh` - Быстрый деплой
Автоматический деплой без вопросов.

```bash
./quick-deploy.sh
```

**Что делает:**
- ⚡ Быстро обновляет код
- ⚡ Устанавливает зависимости
- ⚡ Генерирует Prisma Client
- ⚡ Собирает проект
- ⚡ Перезапускает приложение
- ⚡ Проверяет health check

**Используйте когда:**
- Небольшие обновления
- Нужна скорость
- Уверены, что всё работает

---

### 3. `diagnose.sh` - Диагностика проблем
Проверяет состояние системы и находит проблемы.

```bash
./diagnose.sh
```

**Что проверяет:**
- 🔍 Версии Node.js и npm
- 🔍 Git статус
- 🔍 Переменные окружения (.env)
- 🔍 Prisma Client
- 🔍 Сборку проекта (.next)
- 🔍 Запущенные процессы
- 🔍 Health check endpoint
- 🔍 Порты
- 🔍 Последние логи

**Используйте когда:**
- Что-то не работает
- После деплоя возникли проблемы
- Нужна полная диагностика

---

## 📋 Пошаговая инструкция для продакшн

### Первый деплой

1. **Подключитесь к серверу:**
```bash
ssh user@5.129.242.61
```

2. **Перейдите в директорию проекта:**
```bash
cd /path/to/tipsio
```

3. **Создайте .env файл (если нет):**
```bash
nano .env
```

Добавьте:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/tipsio"
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://5.129.242.61"
```

4. **Запустите полный деплой:**
```bash
./deploy.sh
```

5. **Проверьте результат:**
```bash
curl http://5.129.242.61/api/health | jq .
```

---

### Обновление кода

**Быстрый способ:**
```bash
./quick-deploy.sh
```

**С проверками:**
```bash
./deploy.sh
```

---

### Если что-то не работает

1. **Запустите диагностику:**
```bash
./diagnose.sh
```

2. **Посмотрите логи:**
```bash
# PM2:
pm2 logs tipsio --lines 50

# Docker:
docker logs tipsio-container --tail 50

# systemd:
journalctl -u tipsio -n 50
```

3. **Проверьте health check:**
```bash
curl http://5.129.242.61/api/health | jq .
```

4. **Попробуйте регистрацию:**
```bash
curl -X POST http://5.129.242.61/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123456",
    "venueName": "Test Cafe",
    "venueType": "CAFE"
  }'
```

---

## 🔧 Ручной деплой (если скрипты не работают)

```bash
# 1. Обновить код
git pull origin main

# 2. Установить зависимости
npm ci --production=false

# 3. Сгенерировать Prisma Client
npx prisma generate

# 4. Применить миграции (если нужно)
npx prisma migrate deploy

# 5. Собрать проект
npm run build

# 6. Перезапустить
pm2 restart tipsio
# или
docker-compose restart
# или
sudo systemctl restart tipsio

# 7. Проверить
curl http://5.129.242.61/api/health | jq .
```

---

## 🐛 Решение частых проблем

### Проблема: "Prisma Client not generated"
```bash
npx prisma generate
npm run build
pm2 restart tipsio
```

### Проблема: "DATABASE_URL is not set"
```bash
# Проверьте .env
cat .env | grep DATABASE_URL

# Если пусто, добавьте
echo 'DATABASE_URL="postgresql://user:password@localhost:5432/tipsio"' >> .env
```

### Проблема: "Port 3000 already in use"
```bash
# Найдите процесс
lsof -i :3000

# Убейте процесс
kill -9 <PID>

# Или перезапустите PM2
pm2 restart tipsio
```

### Проблема: "Health check failed"
```bash
# Запустите диагностику
./diagnose.sh

# Проверьте логи
pm2 logs tipsio --lines 50

# Проверьте, запущено ли приложение
pm2 status
```

---

## 📊 Мониторинг после деплоя

### Проверка статуса
```bash
pm2 status
```

### Просмотр логов в реальном времени
```bash
pm2 logs tipsio
```

### Проверка использования ресурсов
```bash
pm2 monit
```

### Проверка health check
```bash
watch -n 5 'curl -s http://5.129.242.61/api/health | jq .'
```

---

## 🎯 Чек-лист успешного деплоя

- [ ] Код обновлён (`git pull`)
- [ ] Зависимости установлены (`npm ci`)
- [ ] Prisma Client сгенерирован (`npx prisma generate`)
- [ ] Миграции применены (если нужно)
- [ ] Проект собран (`npm run build`)
- [ ] Приложение перезапущено
- [ ] Health check возвращает 200 OK
- [ ] `prisma.connected = true`
- [ ] `database.userCount` не null
- [ ] Регистрация работает
- [ ] Логи не показывают ошибок

---

## 📞 Поддержка

Если проблема не решается:

1. Запустите `./diagnose.sh` и сохраните вывод
2. Соберите логи: `pm2 logs tipsio --lines 100 > logs.txt`
3. Проверьте health check: `curl http://5.129.242.61/api/health > health.json`
4. Отправьте все файлы для анализа

---

## 🔄 Откат к предыдущей версии

Если новая версия не работает:

```bash
# 1. Откатите код
git log --oneline -5  # Найдите нужный коммит
git reset --hard <commit-hash>

# 2. Пересоберите
npm run build

# 3. Перезапустите
pm2 restart tipsio
```

---

**Удачного деплоя! 🚀**
